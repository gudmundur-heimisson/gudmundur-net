[uwsgi]
plugins = python

base = /www/gudmundur.net
pythonpath = %(base)
virtualenv = %(base)/env

app = app
module = %(app)

socket = %(base)/%n.sock
chown-socket = nginx:nginx
chmod-socket = 644

callable = app
logto = /var/log/uwsgi/%n.log
